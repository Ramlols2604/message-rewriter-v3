import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "ProDraft AI - Clear. Professional. Compliant.",
  description: "Transform informal or unclear messages into polished, workplace-ready communication. Preserves intent, applies tone and formatting rules, and evaluates risk.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
